#include <stdio.h>

int main()
{
    // We know this the convenient way to print a integer value
    int a = 6;
    printf("%d\n", a);

    // We know this the convenient way to print a string
    char str[] = "i like you";
    printf("%s\n", str);
    
    // This is also a string but declare by a pointer hehehehehehe🤩🤩
    char *str2 = "I like you too";
    printf("%s\n",str2);
    return 0;
}
